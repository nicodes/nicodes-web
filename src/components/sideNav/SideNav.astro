---
import styles from "./sideNav.module.scss";
---

<nav class={styles.fixedContainer}>
  <div class={styles.relativeContainer}>
    <div
      id="drawer"
      class={styles.drawer}
      style="transform: translateX(-100%); opacity: 0"
    >
      <div class={styles.scroll}>
        <p>About</p>
        <p>Portfolio</p>
        <p>Github</p>
        <p>Linkedin</p>
        {Array.apply(null, Array(400)).map((e) => <p>drawer</p>)}
      </div>
      <div class={styles.borderContainer}>
        <div class={styles.border}></div>
      </div>
    </div>
  </div>
</nav>

<script>
  const headerButton = document.getElementById("header-button");
  const drawer = document.getElementById("drawer");

  function toggleDrawer() {
    if (drawer?.style?.transform === "translateX(-100%)") {
      drawer.style.transform = "translateX(0)";
      drawer.style.opacity = "1";
    } else if (drawer?.style?.transform) {
      drawer.style.transform = "translateX(-100%)";
      drawer.style.opacity = "0";
    }
  }

  headerButton?.addEventListener("click", toggleDrawer);
</script>
