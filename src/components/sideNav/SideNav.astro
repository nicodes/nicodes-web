---
import styles from "./sideNav.module.scss";
---

<div class={styles.fixedContainer}>
  <div class={styles.relativeContainer}>
    <button id="button1" class={styles.button}>button1</button>
    <div
      id="drawer"
      class={styles.drawer}
      style="transform: translateX(-100%); opacity: 0"
    >
      <div class={styles.scroll}>
        <button id="button2" class={styles.button}>button2</button>
        <p>About</p>
        <p>Portfolio</p>
        <p>Github</p>
        <p>Linkedin</p>
        <!-- {Array.apply(null, Array(400)).map((e) => <p>drawer</p>)} -->
      </div>
      <div class={styles.borderContainer}>
        <div class={styles.border}></div>
      </div>
    </div>
  </div>
</div>

<script>
  const button1 = document.getElementById("button1");
  const button2 = document.getElementById("button2");
  const drawer = document.getElementById("drawer");

  function toggleDrawer() {
    if (drawer?.style?.transform === "translateX(-100%)") {
      drawer.style.transform = "translateX(0)";
      drawer.style.opacity = "1";
    } else if (drawer?.style?.transform) {
      drawer.style.transform = "translateX(-100%)";
      drawer.style.opacity = "0";
    }
  }

  button1?.addEventListener("click", toggleDrawer);
  button2?.addEventListener("click", toggleDrawer);
</script>
